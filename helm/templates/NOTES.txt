myapp has been deployed.

Check the status of the pods with:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Forward the service to your machine to test it locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "myapp.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/health

{{- if .Values.ingress.enabled }}
Ingress is enabled, the app should be reachable at:
  http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- end }}
